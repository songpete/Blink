%h2 Most Often Used Links

%br
%table.table-striped.table-condensed
  %tr
    %th Rank
    %th Short path
    %th Destination
    %th Times used
    %th

  - @sites.each_with_index do |site, idx|
    %tr
      %td= idx + 1
      %td= link_to site.short_path, request.host_with_port + '/' + site.short_path
      %td= site.destination
      %td= site.count
      %td= link_to 'Show', site

%br

= link_to 'Make New Link', root_path
